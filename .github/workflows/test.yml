name: 🧪 Tests and Quality Checks

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: 🧪 Run Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🐍 Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    
    - name: ⚡ Install uv
      uses: astral-sh/setup-uv@v2
      with:
        version: "latest"
    
    - name: 📦 Install dependencies
      run: |
        uv sync
    
    - name: 🔍 Lint with flake8
      run: |
        # Критические ошибки
        uv run flake8 src tests --count --select=E9,F63,F7,F82 --show-source --statistics
        # Все предупреждения
        uv run flake8 src tests --count --max-complexity=10 --max-line-length=88 --statistics
    
    - name: 🔍 Type check with mypy
      env:
        TELEGRAM_BOT_TOKEN: test_bot_token_for_ci
        OPENROUTER_API_KEY: test_api_key_for_ci
      run: |
        uv run mypy src --ignore-missing-imports
      continue-on-error: true  # Не прерываем CI на ошибках типизации в MVP
    
    - name: ✨ Check code formatting with black
      run: |
        uv run black --check --diff src tests
      continue-on-error: true  # Не прерываем CI на форматировании в MVP
    
    - name: 🧪 Run tests with pytest
      env:
        TELEGRAM_BOT_TOKEN: test_bot_token_for_ci
        OPENROUTER_API_KEY: test_api_key_for_ci
        OPENROUTER_MODEL: test/model
        LLM_TIMEOUT: 5
        LLM_TEMPERATURE: 0.8
        LLM_RETRY_ATTEMPTS: 3
        LOG_LEVEL: INFO
        LOG_FILE: logs/test.log
        DEBUG: false
      run: |
        uv run pytest tests/ -v --tb=short --cov=src --cov-report=xml --cov-report=term-missing
    
    - name: 📊 Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false  # Не прерываем CI если codecov недоступен

  docker-test:
    name: 🐳 Docker Build Test
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: 🐳 Build Docker image
      run: |
        docker build -t sarcastic-bot:test .
    
    - name: 🧪 Test Docker container initialization
      run: |
        # Создаем тестовый .env файл
        echo "TELEGRAM_BOT_TOKEN=test_token" > .env
        echo "OPENROUTER_API_KEY=test_key" >> .env
        
        # Проверяем что контейнер может запуститься и инициализироваться
        # Копируем тестовый скрипт в контейнер и запускаем его
        docker run --env-file .env sarcastic-bot:test python /app/test_container_init.py
        
        # Проверяем что образ создался корректно
        docker images | grep sarcastic-bot

  security-scan:
    name: 🔒 Security Scan
    runs-on: ubuntu-latest
    needs: test
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
    
    - name: ⚡ Install uv
      uses: astral-sh/setup-uv@v2
      with:
        version: "latest"
    
    - name: 📦 Install dependencies
      run: |
        uv sync
    
    - name: 🔒 Run safety check
      run: |
        uv add --dev safety
        uv run safety check
      continue-on-error: true  # Не прерываем CI на уязвимостях в dev зависимостях
